<h1>商品詳細</h1>
<div>
  <%= attachment_image_tag @item, :image, :fill, 100, 100, format: 'jpeg' %>
  <p>商品名：<%= @item.name %></p>
  <p>商品説明：<%= @item.introduction %></p>
  <p>ジャンル：<%= @item.genre_id %></p>
  <p>税込価格（税抜価格）：**（<%= @item.price %>）円</p>
  <p>ステータス：<%= @item.is_active %></p>
</div>

<%= link_to "編集する", edit_admin_item_path(@item) %>