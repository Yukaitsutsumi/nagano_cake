<h3>注文履歴一覧</h3>
<% @orders.each do |order| %>
<div>
  <p>注文日：<%= order.created_at %></p>
  <p>発送先：〒<%= order.postal_code %> <%= order.address %> <%= order.name %></p>
  <p>注文商品：<%=  %></p>
  <p>支払金額：<%= order.total_payment %></p>
  <p>ステータス：入金確認</p>
  <p>注文詳細：<a href="##">表示する</a></p>
</div>
<% end %>