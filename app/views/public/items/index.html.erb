<h1>商品一覧</h1>
<% @items.each do |item| %>
<div>
  <p><%= attachment_image_tag item, :image, :fill, 100, 100, format: 'jpeg' %></p>
  <p><%= item.name %></p>
  <p>￥<%= item.price %></p>
</div>
<% end %>